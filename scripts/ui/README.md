# /scripts/ui — UI 脚本

> 与 `/scenes/ui` 配套的 UI 逻辑脚本。

## 命名约定
- 类名：`UI<功能>`（如 `UIMainMenu`）
- 文件名：`ui_<功能>.gd`（如 `ui_main_menu.gd`）

## 预期 UI 脚本

| 脚本 | 对应场景 |
|---|---|
| `ui_main_menu.gd` | `ui_main_menu.tscn` |
| `ui_dialogue_box.gd` | `ui_dialogue_box.tscn` |
| `ui_stats_panel.gd` | `ui_stats_panel.tscn` |
| `ui_time_indicator.gd` | `ui_time_indicator.tscn` |

**当前为空**，等待 UI 实现完成后填充。